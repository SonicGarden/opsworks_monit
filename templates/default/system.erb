check system localhost
  if loadavg (5min) > 2 then alert
  if swap usage > 60% then alert

# for OS layer
check device datafs-os with path /dev/xvda1
  if space usage > 80% for 5 cycles then alert
  if inode usage > 90% then alert
